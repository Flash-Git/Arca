import React, { useEffect } from "react";

import TradeBox from "../tradeBox/Box";

const Home = () => {
  useEffect(() => {
    //eslint-disable-next-line
  }, []);

  return (
    <div>
      <TradeBox />
    </div>
  );
};

export default Home;
